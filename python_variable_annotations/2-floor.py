#!/usr/bin/env python3
"""This module contains a type-annotated function floor which takes a
 float n as argument and returns the floor of the float as an integer.

"""


def floor(n: float) -> int:
    """Returns the floor of a float as an integer."""
    return int(n)
